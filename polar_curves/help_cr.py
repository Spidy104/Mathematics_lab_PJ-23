# from pylab import *
#
# rads = arange(1, 100)
# a = randint(1, 6)
# x = a * (cos(rads) - (rads * sin(rads)))
# y = a * (sin(rads) - (rads * cos(rads)))
#
# plot(x, y)
# title("Involute of a circle")
# show()
